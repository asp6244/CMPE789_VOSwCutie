defaults:
  - datasets

pre_training:
  datasets:
    - FSS
    - DUTS_TR
    - DUTS_TE
    - ECSSD
    - BIG
    - HRSOD

main_training:
  datasets:
    - MOTS
  num_iterations: 100
  # lr_schedule_steps: [140000, 160000]
